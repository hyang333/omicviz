% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vizVolcano.R
\name{vizVolcano}
\alias{vizVolcano}
\title{Create a Volcano Plot from DESeq2 Results}
\usage{
vizVolcano(
  res,
  lfc_threshold = 1,
  padj_cutoff = 0.05,
  col_up = "#E41A1C",
  col_low = "#6BAED6",
  col_ns = "grey70",
  point_size = 1.2,
  point_alpha = 0.7,
  title = "Volcano Plot",
  xlab = "log2(Fold Change)",
  ylab = "-log10(adjusted p-value)",
  label_genes = NULL,
  gene_col = "symbol"
)
}
\arguments{
\item{res}{A \code{data.frame} (or tibble) of DESeq2 results. Must contain
columns \code{log2FoldChange} and \code{padj}. Typically produced by
\code{DESeq2::results()} or read from a saved CSV.}

\item{lfc_threshold}{Numeric. The log2 fold-change threshold used to
separate "large" from "small" significant changes. Default is \code{1}.}

\item{padj_cutoff}{Numeric. Adjusted p-value cutoff for significance.
Default is \code{0.05}.}

\item{col_up}{Character. Color for significant genes with
\code{|log2FoldChange| > lfc_threshold}. Default is \code{"#E41A1C"}
(red).}

\item{col_low}{Character. Color for significant genes with
\code{|log2FoldChange| <= lfc_threshold}. Default is \code{"#6BAED6"}
(light blue).}

\item{col_ns}{Character. Color for non-significant genes.
Default is \code{"grey70"}.}

\item{point_size}{Numeric. Size of the points. Default is \code{1.2}.}

\item{point_alpha}{Numeric. Transparency of the points (0--1).
Default is \code{0.7}.}

\item{title}{Character. Plot title. Default is \code{"Volcano Plot"}.}

\item{xlab}{Character. X-axis label. Default is
\code{"log2(Fold Change)"}.}

\item{ylab}{Character. Y-axis label. Default is
\code{"-log10(adjusted p-value)"}.}

\item{label_genes}{Character vector of gene symbols to label on the plot,
or \code{NULL} (no labels). Default is \code{NULL}.}

\item{gene_col}{Character. Column name in \code{res} that holds gene
symbols, used when \code{label_genes} is not NULL.
Default is \code{"symbol"}.}
}
\value{
A \code{ggplot} object (invisibly). The plot is drawn as a side
effect.
}
\description{
Generates a publication-quality volcano plot from DESeq2 differential
expression results. Genes are colored by significance and fold-change
magnitude:
\itemize{
\item \strong{Red}: significant (adjusted p < \code{padj_cutoff}) and
\code{|log2FoldChange| > lfc_threshold} (large change)
\item \strong{Light blue}: significant but
\code{|log2FoldChange| <= lfc_threshold} (small change)
\item \strong{Grey}: not significant
}
}
\details{
The number of genes in each category is displayed in the plot legend.
}
\examples{
\dontrun{
res <- read.csv("EUC021_LFC_AL.csv")
vizVolcano(res, lfc_threshold = 1)
vizVolcano(res, lfc_threshold = 0.5, padj_cutoff = 0.01)
}

}
